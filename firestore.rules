rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Default Deny
    match /{document=**} {
      allow read, write: if false;
    }

    // Global Helpers
    function isSignedIn() {
      return request.auth != null;
    }

    function isOwner(uid) {
      return isSignedIn() && request.auth.uid == uid;
    }

    function incoming() {
      return request.resource.data;
    }

    function existing() {
      return resource.data;
    }

    function isAtomicViewIncrement() {
      return incoming().diff(existing()).affectedKeys().hasOnly(['views', 'lastViewedAt']) &&
             incoming().views == existing().get('views', 0) + 1;
    }

    // ==========================================
    // Normalized Data Architecture
    // ==========================================
    
    // 1. Private Drafts (Normalized Subcollections)
    match /users/{uid} {
      allow read: if isOwner(uid);
      
      match /invites/{inviteId} {
        allow read, write: if isOwner(uid);
        
        match /pages/{pageId} {
          allow read, write: if isOwner(uid);
          
          match /elements/{elementId} {
            allow read, write: if isOwner(uid);
          }
        }
      }
      
      // Legacy Adapter Path (Read-only mapped or fully manageable by owner)
      match /events/{eventId} {
        allow read, write: if isOwner(uid);
      }
    }

    // 2. Public Projections & Snapshots
    match /public_invites/{inviteId} {
      // NOTE: Password protection secure payload handling deferred pending mechanism definition
      allow read: if true; 
      allow create: if isSignedIn() && incoming().ownerId == request.auth.uid;
      allow update: if isSignedIn() && existing().ownerId == request.auth.uid && incoming().ownerId == request.auth.uid;
      allow delete: if isSignedIn() && existing().ownerId == request.auth.uid;
      
      match /versions/{snapshotId} {
        allow read: if true;
        allow write: if false;
      }
      
      match /insights/general {
        allow read: if true;
        allow create: if true; // Initial creation
        allow update: if isAtomicViewIncrement();
      }

      match /rsvps/{rsvpId} {
        allow read: if true;
        allow create: if incoming().guestName is string && 
                         incoming().guestName.size() > 0 && 
                         incoming().guestName.size() <= 100 &&
                         incoming().attending is bool &&
                         incoming().guestCount is number &&
                         incoming().guestCount >= 0 &&
                         incoming().guestCount <= 50;
        // Only owner can update/delete guest RSVPs
        allow update, delete: if isOwner(get(/databases/$(database)/documents/public_invites/$(inviteId)).data.ownerId);
      }
      
      match /transactions/{txId} {
        allow read: if true;
        allow create: if incoming().senderName is string &&
                         incoming().senderName.size() > 0 &&
                         incoming().senderName.size() <= 100 &&
                         incoming().amount is number &&
                         incoming().amount >= 0;
        allow update, delete: if isOwner(get(/databases/$(database)/documents/public_invites/$(inviteId)).data.ownerId);
      }
    }
    
    // Legacy published path
    match /published_events/{slug} {
      allow read: if true;
      allow write: if false;
      
      match /{subPath=**} {
         allow read: if true;
      }
    }

    // Connection test document
    match /test/connection {
      allow read: if true;
    }
  }
}
